# UX-01 · executed usability pass

Assignment: [#21](https://github.com/hynk-studio/CodaBridge/issues/21). **Agent/expert walkthrough completed locally; physical-device and human acceptance remain awaiting the owner.** This was not a naive-human study. No participant scores or comprehension/listening claims were collected.

Baseline main was `c10f1adb35c9729550adc97a5974696cf65008fa`, tree `241f5f109a56bf672cb5f3a1fc23e91ad2314515`. A separate worktree, `codex/ux-01-usability`, preserved the original checkout and its unrelated untracked image. Application changes are committed at `d38fe7fee4b568ebe73452c0543e760ed6aecb8f`; the Draft PR records its exact final evidence head. Only `Exchange.tsx` and its sealed browser tests changed among existing files.

The baseline was built and served before app edits, at `http://127.0.0.1:4173`. Fresh disposable Chromium contexts represented desktop A (1440×1000), phone-emulated B (390×844, touch), and cold receivers C/D. The browser connector exposed the owner's Chrome profile and one in-app session; neither was used for these profiles. Visible buttons, labels, selectors and disclosures drove the walkthrough from home. No test IDs, injected Composer state or app-function calls completed the baseline tasks. File chooser plumbing and TEST ONLY key retention were automated and are assistance, not OS picker or human key-management evidence.

Node 25.9.0, npm 11.12.1, Playwright 1.63.0, Chromium 153.0.8010.12, macOS 26.6.2 arm64, Python 3.9.6. Baseline and final browsers at port 4173 reported actual `isSecureContext: true`, native `crypto.subtle` and `getRandomValues`; successful encryption/decryption exercised those APIs. Credentials were excluded. All three safe model probes returned 503 / `NOT_CONFIGURED`. No model call or hosting occurred.

| Goal | Executed baseline path and result | After the fixes |
| --- | --- | --- |
| Make something personal | Home → Play original 1.wav → Make my version → lengthen ×1.25 (desktop: 1.578 → 1.973 s) / first-gap +0.05 s (phone: 1.578 → 1.628 s) → Undo/Redo → personal Korean/emoji label → Save & codebook → project download. Desktop also saved its block to the codebook. Original and synthetic Play states were observed; sound quality was not judged. | The actual saved project reopened through the UI. Meaning, edited timing and Undo/Redo survived Exchange navigation. No Composer code changed. |
| Inspect the reference and return | Compare & ask → Observed timing reference → expand nearest actual references → inspect row 2412, six clicks, 1.248261 s → Return to my Composer. Both profiles retained edits/labels and available Undo. The panel explicitly rejects authenticity/meaning interpretations. | Existing Exchange regression also passed Atlas comparison and both Prediction Reveal-reset paths. No Atlas/Prediction code or artifact changed. |
| Ordinary file, cold reply | Two-block Composer → selected-block transmission with whole-phrase unchecked → plain A1 (one block) → Download coda JSON → cold phone C opens actual bytes → choose 7.wav → B1 → download → A opens returned file and explicitly replaces. All history is visibly disclosed. | Reopened baseline A1, chose distinct 2.wav, finalized a new B1 branch, downloaded it and opened it in another fresh phone context. Both legitimate branches independently verify against A1. |
| Sealed A1/B1/A2 | Reset → choose sealed before text → whole Composer phrase (two blocks) → Finalize → Prepare → explicit Reveal/Select/Hide, separate test-key retention and acknowledgement → download. Cold D opens A1, fails with a well-formed wrong key, succeeds with its matching key, replies from 2.wav. A rejects the old key on B1, unlocks with the new key, explicitly replaces, then creates A2. Three keys were distinct. | Full fresh A1/B1/A2 repeated. File identifiers matched actual download names and the cold locked preview. Candidate/current identifiers remained distinct; obsolete identifiers disappeared after finalizing a reply. Independent Node/Python checks passed on all retained final files. |
| Recover and resume | Copy denial remained masked and did not acknowledge saving. Explicit Reveal/Select/Hide worked. Unfinished Korean/emoji notes survived cancellation and workspace return. Rejected project import retained the note. Cancelled lock retained work; explicit lock/re-unlock and refresh/file reopening worked on desktop and phone. | Phone copy feedback is visible without a backtrack. Repeated ciphertext download stayed byte-identical. Note cancellation/navigation, rejected import, lock/re-unlock and refresh/file reopening passed again. |

[Original 135 recorded control actions](baseline-steps.json) ran from 04:07:39 to 04:13:26 UTC on 2026-09-14; native desktop attempts were additional. [After action log](after-steps.json) records the recheck. Durations are automation action timings, including tool overhead, not human task-time benchmarks. Loading audio and loading the source-checked Atlas were visible waits; no precise load-latency estimate was collected. The only UI backtrack caused by the three findings was scrolling upward to locate phone copy-denial guidance. No help from a human was used; expert familiarity with the app and automated file/key plumbing limit the inference.

| Finding classification | Original observation | Bounded correction |
| --- | --- | --- |
| F1 · observed friction | Sealed Finalize said “Download the file” while Download was disabled and Prepare was still required. | The status names Prepare, separate key retention, then Download. Plain-mode guidance remains unchanged. |
| F2 · observed friction | Prepared-key and locked-candidate panels omitted the filename; the cleared chooser said “No file chosen.” Matching depended on the harness's separate file/key bookkeeping. | Display the existing canonical encrypted download name beside the key and in locked/candidate views. It survives a local rename and is a matching aid, not authentication or a new format/digest. |
| F3 · observed friction | At 390×844, the copy-denial alert was entirely above the viewport (`y=-109.45`, height 102). The key controls showed no local result. | Put success/failure feedback beside Copy and scroll it into view without moving focus or selecting/revealing the key. After: `y=611.88`, height 102; masked, acknowledgement unchecked. Existing generation/display guards suppress stale results. |

| Before / after captures | Before | After |
| --- | --- | --- |
| Sealed next step · desktop | [Base replay](captures/finalize-guidance-before.png) | [Final build](captures/finalize-guidance-after.png) |
| Copy denial · phone emulation | [Base replay](captures/phone-copy-before.png) | [Final build](captures/phone-copy-after.png) |
| Locked-file identification · phone emulation | [Base replay](captures/phone-locked-before.png) | [Final build](captures/phone-locked-after.png) |

The first browser runner cleared its disposable `test-results` directory, removing the initial captures/logs. The original step log and browser download bytes were recovered, including both initially colliding Composer filenames. Before captures above are explicitly **replays**, made from a fresh unchanged-base build on loopback port 4174; they reproduce the original findings and are not the original task screenshots. Baseline success/failure observations were not rewritten. A subsequent REPL recovery timed out; retained reproducible sealed fixtures therefore come from the final rerun. Evidence now lives outside the runner's cleanup directory. An attempted `npm run preview -- --port 4174` was rejected because the script already specifies a port; the replay used the same local Wrangler command with one explicit port. These were harness/setup issues, not app defects.

| Evidence layer | Actual status |
| --- | --- |
| Agent walkthrough | Executed on desktop and narrow touch emulation with invented PUBLIC TEST ONLY Korean/emoji content. Programmatic fill does not verify a physical keyboard/IME. |
| Regression | Typecheck/lint/build passed; 249 unit, 12 sealed unit, 56 sealed browser and 16 Exchange browser cases passed. Includes focused new guidance/identifier tests and visible, focused copy feedback. |
| Actual desktop clipboard | Not tested. Clipboard-write permission was denied in disposable contexts to avoid modifying the owner's clipboard. Success/missing-API/delayed outcomes are regression simulations. |
| Real browser tab lifecycle | Headed Chrome for Testing and native AX tab switching were attempted. The tab selection visibly changed, but `document.hidden` remained false / `visibilityState` visible; no genuine hidden-state remasking pass is claimed. Explicit Hide was used afterward. The regression's hidden-state simulation passed separately. |
| Native sharing / file picker | Headless share unavailable. Headed `canShare` enabled the button, but Playwright and native AX clicks returned the bounded sharing failure before a chooser appeared. Cancellation was therefore not observed; no recipient was selected, no fallback download occurred. Regression share cancellation is simulated. Chooser APIs supplied actual files. |
| Hardware / other engines | USB inventory returned no devices; ADB was absent from PATH and the usual Android SDK path. Safari is installed but its owner profile was not used. Playwright WebKit/Firefox and the Firefox app were absent. Physical phone remains awaiting owner. |
| Human listening / usability | Awaiting owner: volume comfort, perceptible rhythm changes, Downloads discovery, key matching, real copying/background/share cancellation and subjective ownership. No independent-human count or comprehension pass. |
| Hosting | Not run. Both owned previews and temporary browsers were stopped at closeout. No LAN listener, tunnel, security flag change, certificate install, deployment or inference enablement. |

[Exact commands/results](checks/commands.json) · [fresh served build and endpoint refusals](checks/served.json) · [sealed artifact checks](checks/sealed-artifacts.txt) · [baseline plain branch](checks/plain-baseline.json) · [after plain branch](checks/plain-after.json) · [exact prefix/repeated-file checks](checks/prefix-preservation.json).

The fresh final client is `index-B2liexg8.js`, 487,121 bytes, SHA-256 `e8fe47853b916d395443a809f371c1086cdb9406b9e34dab31b36c713a29a8dd`. Baseline client was `index-fVX2loRR.js`, SHA-256 `277e897f521c6c687a47e54be6dd6496d6c62cad07e8f7c6d04409e917b3382c`. CSS and Worker bytes are unchanged. The final served-byte probe matches the build exactly. [Preservation comparison](preservation.json) proves **879 existing base files byte-identical**, including all historical encrypted files/public keys, fixtures, old captures/logs, source/media and research/Atlas files. This is delivery evidence, not a future application freeze.

All reproducibility material is conspicuously [PUBLIC TEST ONLY](public-test-only/README.md), with separate keys and [byte identities](artifact-identities.json). Final expected plaintext was obtained through separately confirmed UI exports for verification, after the normal ciphertext journeys. A first plain-verifier invocation incorrectly treated two alternative B1 replies as one sequential chain and correctly rejected “prior turns changed”; each A1→B1 branch then passed separately. No checker or format was changed to pass it.

The full unrelated suite, fitting, metadata replay and deployment were not run. No owner's real message, opening key, browser storage or clipboard content was inspected. See the [verified start/stop route and pending 10–15 minute owner checklist](OWNER_CHECK.md). **Next owner action: connect the Android phone by USB and approve this Mac's debugging/trust prompt when ready for that check.**
